type Blog = record {
    title: text;
    data: nat64;
    content: text;
    tags: vec text;
};

type Result = variant { Ok : text; Err : text; };

service : {
    "add_blog": (text, text, vec text) -> ();
    "get_blogs": () -> (vec Blog) query;
}